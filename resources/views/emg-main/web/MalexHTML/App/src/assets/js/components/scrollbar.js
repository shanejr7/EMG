//=================================================
// Scrollbar
//=================================================

( function( $ ) {
    'use strict';

    // Theme: Light
    $( '.scrollbar-light' ).mCustomScrollbar( {
        theme: 'light'
    } );

    // Theme: Dark
    $( '.scrollbar-dark' ).mCustomScrollbar( {
        theme: 'dark'
    } );
} )( jQuery );
